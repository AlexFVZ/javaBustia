CREATE TABLE IF NOT EXISTS Preu(
preu TEXT,
diaSessio TEXT,
codiZona INTEGER,
FOREIGN KEY (diaSessio) REFERENCES Sessio(dia),
FOREIGN KEY (codiZona) REFERENCES Zona(codi)
);

select:
SELECT * FROM Preu;

insert:
INSERT INTO Preu(preu,diaSessio,codiZona) VALUES (?,?,?);